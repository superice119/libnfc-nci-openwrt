Index: libnfc-nci-2.0/src/android/utility/android-base/file.h
===================================================================
--- libnfc-nci-2.0.orig/src/android/utility/android-base/file.h
+++ libnfc-nci-2.0/src/android/utility/android-base/file.h
@@ -48,7 +48,7 @@ bool ReadFully(int fd, void* data, size_
 // get modified. This means that ReadFullyAtOffset can be used concurrently with other calls to the
 // same function, but concurrently seeking or reading incrementally can lead to unexpected
 // behavior.
-bool ReadFullyAtOffset(int fd, void* data, size_t byte_count, off64_t offset);
+bool ReadFullyAtOffset(int fd, void* data, size_t byte_count, off_t offset);
 bool WriteFully(int fd, const void* data, size_t byte_count);
 bool RemoveFileIfExists(const std::string& path, std::string* err = nullptr);
 #if !defined(_WIN32)
@@ -63,4 +63,4 @@ std::string Basename(const std::string&
 std::string Dirname(const std::string& path);
 }  // namespace base
 }  // namespace android
-#endif // ANDROID_BASE_FILE_H
\ No newline at end of file
+#endif // ANDROID_BASE_FILE_H
Index: libnfc-nci-2.0/src/android/utility/base/file.cpp
===================================================================
--- libnfc-nci-2.0.orig/src/android/utility/base/file.cpp
+++ libnfc-nci-2.0/src/android/utility/base/file.cpp
@@ -157,7 +157,7 @@ static ssize_t pread(int fd, void* data,
   return static_cast<ssize_t>(bytes_read);
 }
 #endif
-bool ReadFullyAtOffset(int fd, void* data, size_t byte_count, off64_t offset) {
+bool ReadFullyAtOffset(int fd, void* data, size_t byte_count, off_t offset) {
   uint8_t* p = reinterpret_cast<uint8_t*>(data);
   while (byte_count > 0) {
     ssize_t n = TEMP_FAILURE_RETRY(pread(fd, p, byte_count, offset));
